# Core numerics / analysis
numpy>=1.26
pandas>=2.1
scipy>=1.11            # Wilcoxon signed-rank, Spearman, bootstrap helpers
matplotlib>=3.8
seaborn>=0.13

# ML: train the ensemble in PyTorch; export weights for the C++ engine
torch>=2.2

# Experiments
pyyaml>=6.0            # configs/*.yaml
tqdm>=4.66
joblib>=1.3            # parallel runs


# Optional
jupyter>=1.0
